# hero
hero practise projrct

## Animated preview

Below is a short animated preview of the Mini Group demo landing page. It shows a product card, the progress bar filling, and contributor avatars appearing — useful as a README capture for demos.

![Mini Group demo preview](assets/preview-animation.svg)

Open `index.html` and run the local demo server to interact with the live version:

```bash
npm install
npm run start
# then open http://localhost:3000
```
